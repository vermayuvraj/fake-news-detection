# data

Put the two dataset files here before running:

```
data/Fake.csv
data/True.csv
```

They aren't included in the submission because they're large (~116 MB). Get them
from the Kaggle link in the main README (either download manually and copy the
two CSVs here, or run `python scripts/download_data.py`).
